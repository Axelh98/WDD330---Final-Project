export function renderDashboard(container) {
    console.log("Dashboard Rendering...");
    container.innerHTML = `
        <h1>Dashboard</h1>
        <p>This is the dashboard view.</p>
        <p>Here will be some widgets or components to display the data.</p>
    `;
}